# Migration 7 关键修复记录

- 修复 `rt_scalper.py` 缩进错误，恢复全项目可编译。
- 平仓/减仓只有在交易所持仓确认后才修改本地状态。
- 平仓或减仓结果未知时使用 `CLOSE_PENDING` / `REDUCE_PENDING`。
- 修复市场结构快照中未定义的 `precomputed_breadth`。
- 统一市场结构代码读取 `timestamp` 字段。
- 市场结构保护层异常时禁止新开仓，不再恢复满风险。
- 新仓止损创建失败时立即尝试紧急平仓，不再将其视为可忽略。
- 默认配置明确为纯实盘。

> 注意：历史测试网和 paper trading 兼容代码仍存在于旧模块中，但默认主配置为实盘。本补丁优先修复会造成真实仓位与本地状态脱节的 P0 问题。
